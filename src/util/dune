(ocamllex
  (modules CompactLexer))

(menhir
  (modules CompactParser))

(library
 (name scilla_util)
 (wrapped false)
 (synopsis "[Internal] Utility library of Scilla")
 (libraries core_kernel fileutils))
